name: Sync to GitLab

on:
  push:
    branches:
      - '**'

jobs:
  call-sync:
    uses: AGES-RFID/cicd/.github/workflows/sync-to-gitlab.yml@main
    secrets:
      gitlab_token: ${{ secrets.GITLAB_TOKEN }}